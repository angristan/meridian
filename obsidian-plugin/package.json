{
  "name": "@meridian/obsidian-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && esbuild src/main.ts --bundle --external:obsidian --format=cjs --platform=browser --target=es2022 --outfile=dist/main.js && cp ../manifest.json dist/manifest.json && cp src/styles.css dist/styles.css && cd dist && zip -q meridian-sync.zip main.js manifest.json styles.css",
    "package": "bun run build",
    "test": "vitest run",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@meridian/crypto": "workspace:*",
    "@meridian/protocol": "workspace:*",
    "@meridian/sync-engine": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "obsidian": "1.13.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
